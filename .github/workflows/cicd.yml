name: Lint & Test 💥

on:
  push:
    branches:
      - main
  schedule:
      - cron: '0 0 * * 0' 
  workflow_dispatch: 

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: 🧼 Checkout Code
        uses: actions/checkout@v3

      - name: 🧠 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 22

      - name: 📦 Install Deps
        run: npm install

      - name: ✨ Lint Code
        run: npm run lint

  test:
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - name: 🧼 Checkout Code
        uses: actions/checkout@v3

      - name: 🧠 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 22

      - name: 📦 Install Deps
        run: npm install

      - name: 🧪 Run Tests
        run: npm run test
